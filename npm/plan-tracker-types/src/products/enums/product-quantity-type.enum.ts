/**
 * Product quantity type.
 */
export enum PRODUCT_QUANTITY_TYPE {
  /** Regulated quantity (with units) */
  REGULATED = "regulated",
  /** Unregulated quantity (no fixed units) */
  UNREGULATED = "unregulated"
}
