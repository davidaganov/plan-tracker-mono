{
  "name": "plans-tracker-root",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-with-tunnel.js",
    "dev:backend": "npm --prefix plan-tracker-back run dev",
    "dev:frontend": "npm --prefix plan-tracker-front run dev",
    "dev:all": "run-p dev:backend dev:frontend",
    "tunnel": "npm --prefix plan-tracker-front run dev:tunnel",
    "lint": "run-s lint:backend lint:frontend",
    "lint:backend": "npm --prefix plan-tracker-back run lint",
    "lint:frontend": "npm --prefix plan-tracker-front run lint",
    "format": "run-s format:backend format:frontend",
    "format:backend": "npm --prefix plan-tracker-back run format",
    "format:frontend": "npm --prefix plan-tracker-front run format",
    "format:check": "run-s format:check:backend format:check:frontend",
    "format:check:backend": "npm --prefix plan-tracker-back run format:check",
    "format:check:frontend": "npm --prefix plan-tracker-front run format:check",
    "install:all": "run-s install:backend install:frontend",
    "install:backend": "npm --prefix plan-tracker-back install",
    "install:frontend": "npm --prefix plan-tracker-front install"
  },
  "devDependencies": {
    "npm-run-all2": "^8.0.4"
  }
}
